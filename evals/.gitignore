# Bench results pile up — only the current `leaderboard.json` is meaningful
# checked in. Historical runs land in `results/` and are gitignored.
results/

# Player-rated calibration set — exported from the dashboard, never
# committed. The bench reads ratings.json; calibration.json is the
# generated report.
ratings.json
calibration.json
traces/
